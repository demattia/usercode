These are modified versions of the macros in the previous directory to produce a scan in one of the variables and a plot of eff(signal)/sqrt(eff(background)).
The samples are specified in RunAll.py and they must be arranged in subdirectories each containing the openhlt_merge.root file.
To run do:
./RunAll.py
