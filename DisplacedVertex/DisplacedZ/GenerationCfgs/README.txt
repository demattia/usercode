The slha files in this dir contain pythia configuration for a displaced Z MC.
They generate neutralino2 -> neutralino1 + Z.

They can be copied in the src/ of CMSSW release and used in conjuction with the scripts in the subdir to run the generation and reconstruction
(including HLT) on CERN batch queues.