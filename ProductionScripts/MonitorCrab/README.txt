The script MonitorCrab.py monitors crab jobs and resubmit them in case of failure.
It provides a succint status report.
Once started it will run indefinitely until stopped.
Usage: edit the script to add the crab directory to monitor at the end:
checkAndResubmit("crabDir/")
